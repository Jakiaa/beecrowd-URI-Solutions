#include<stdio.h>
int main()
{
    #Find distance in relative to other car.
    int distance,time;
    scanf("%d",&distance);
    time=distance*2;
    printf("%d minutos\n",time);
    return 0;
}
